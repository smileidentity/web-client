{
	"name": "@smile_identity/web-page",
	"version": "1.0.0",
	"description": "Self Hosted Integration for Smile Identity on the Web",
	"main": "inline.js",
	"scripts": {
		"build": "for file in src/js/*.js; do esbuild $file --bundle --outfile=build/js/$(basename $file .js).min.js; done",
		"build:dist": "for file in src/js/*.js; do esbuild $file --bundle --minify --outfile=dist/js/$(basename $file .js).min.js; done",
		"clean": "rm -rf build dist",
		"copy": "mkdir -p build && cp -r src/* build && cp cypress/pages/*.html build",
		"copy:dist": "mkdir -p dist && cp -r src/* dist",
		"instrument": "nyc instrument src build",
		"preinstrument": "npm run clean",
		"lint:html": "npx prettier --write $(git ls-files '*.html')",
		"prestart": "npm run clean && npm run copy && npm run build && npm run build:dist",
		"start": "npx serve -p 8000 build",
		"watch": "watch 'npm run prestart' src",
		"test": "npx cypress run"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/smileidentity/hosted-web-integration.git"
	},
	"keywords": [],
	"author": "tams sokari <tams@smileidentity.com>",
	"bugs": {
		"url": "https://github.com/smileidentity/hosted-web-integration/issues"
	},
	"homepage": "https://github.com/smileidentity/hosted-web-integration#readme",
	"devDependencies": {
		"@cypress/code-coverage": "3.12.1",
		"cypress": "^13.3.0",
		"esbuild": "^0.19.4",
		"nyc": "15.1.0",
		"prettier": "^3.0.3",
		"serve": "14.2.1"
	},
	"dependencies": {
		"jszip": "^3.10.1",
		"validate.js": "^0.13.1"
	}
}
